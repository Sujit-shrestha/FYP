{
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Server_Side && nodemon index.js",
    "client": "cd Client_Side && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.10",
    "dotenv": "^16.4.1",
    "path": "^0.12.7"
  }
}
